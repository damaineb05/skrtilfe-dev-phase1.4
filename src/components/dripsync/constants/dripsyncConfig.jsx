/**
 * Constants — DripSync Config
 * Re-export from canonical location for clean import paths.
 *
 * Usage: import { DEFAULT_AVATAR_URL } from '@/components/dripsync/constants/dripsyncConfig'
 */
export * from '../../../constants/dripsyncConfig';